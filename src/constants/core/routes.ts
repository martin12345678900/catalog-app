// import packageName from './config'
const packageName = 'org.reactjs.native.example.ProductsCatalog'
const routes = {
  // Navigators
  RootNavigator: `${packageName}.rootNavigator`,

  // Screens
  Catalog: `${packageName}.catalog`,
  Details: `${packageName}.details`,
  Cart: `${packageName}.cart`
}

export default routes
